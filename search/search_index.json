{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Hawk SDK","text":"<p>Source Code</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install hawk-sdk\n</code></pre> <p>Authentication (one of): <pre><code>export GOOGLE_APPLICATION_CREDENTIALS=\"/path/to/service_account.json\"\n</code></pre> <pre><code>export SERVICE_ACCOUNT_JSON='{\"type\":\"service_account\",\"project_id\":\"...\"}'\n</code></pre></p> <p>Note</p> <p>All timestamps are in UTC.</p>"},{"location":"#api-reference","title":"API Reference","text":"UniversalUniversalSupplementalSystemDataObject <p>Query any combination of hawk_ids and field_ids.</p> <pre><code>from hawk_sdk.api import Universal\nuniversal = Universal(environment=\"production\")\n</code></pre> Method Description <code>get_data(hawk_ids, field_ids, start_date, end_date, interval)</code> Fetch data (use <code>interval='snapshot'</code> for point-in-time) <code>get_latest_snapshot(hawk_ids, field_ids)</code> Fetch most recent data available <code>get_field_ids(field_names)</code> Lookup field_ids by name <code>get_all_fields()</code> List all available fields <p>get_data <pre><code>def get_data(hawk_ids: List[int], field_ids: List[int], start_date: str, end_date: str, interval: str) -&gt; DataObject\n</code></pre></p> Parameter Type Description <code>hawk_ids</code> <code>List[int]</code> Hawk IDs to query <code>field_ids</code> <code>List[int]</code> Field IDs to retrieve <code>start_date</code> <code>str</code> Start date (<code>YYYY-MM-DD</code>). Ignored for snapshot. <code>end_date</code> <code>str</code> End date (<code>YYYY-MM-DD</code>) or timestamp (<code>YYYY-MM-DD HH:MM:SS</code>) for snapshot <code>interval</code> <code>str</code> Data interval: <code>1d</code>, <code>1h</code>, etc. Use <code>snapshot</code> for point-in-time <p>get_latest_snapshot <pre><code>def get_latest_snapshot(hawk_ids: List[int], field_ids: List[int]) -&gt; DataObject\n</code></pre></p> <p>Returns DataFrame with columns: <code>date</code>, <code>hawk_id</code>, <code>ticker</code>, plus one column per field. Missing values are <code>NaN</code>.</p> <p>get_field_ids <pre><code>def get_field_ids(field_names: List[str]) -&gt; DataObject\n</code></pre></p> <p>get_all_fields <pre><code>def get_all_fields() -&gt; DataObject\n</code></pre></p> <p>Query supplemental data not tied to hawk_ids (identified by source and series_id).</p> <pre><code>from hawk_sdk.api import UniversalSupplemental\nsupplemental = UniversalSupplemental(environment=\"production\")\n</code></pre> Method Description <code>get_data(sources, series_ids, start_date, end_date)</code> Fetch data for specific series <code>get_data_by_source(sources, start_date, end_date)</code> Fetch all series for given sources <code>get_latest_data(sources, series_ids)</code> Fetch most recent data for each series <code>get_all_series(source=None)</code> List available series metadata <code>get_available_sources()</code> List available data sources <p>get_data <pre><code>def get_data(sources: List[str], series_ids: List[str], start_date: str, end_date: str) -&gt; DataObject\n</code></pre></p> Parameter Type Description <code>sources</code> <code>List[str]</code> Data source identifiers (e.g., <code>['eia_petroleum']</code>) <code>series_ids</code> <code>List[str]</code> Series codes (e.g., <code>['WCESTUS1', 'WCRFPUS2']</code>) <code>start_date</code> <code>str</code> Start date (<code>YYYY-MM-DD</code>) <code>end_date</code> <code>str</code> End date (<code>YYYY-MM-DD</code>) <p>get_data_by_source <pre><code>def get_data_by_source(sources: List[str], start_date: str, end_date: str) -&gt; DataObject\n</code></pre></p> <p>Fetches all series for the given sources without specifying individual series_ids.</p> <p>get_latest_data <pre><code>def get_latest_data(sources: List[str], series_ids: List[str]) -&gt; DataObject\n</code></pre></p> <p>Returns the most recent data point for each specified series.</p> <p>get_all_series <pre><code>def get_all_series(source: Optional[str] = None) -&gt; DataObject\n</code></pre></p> <p>Returns DataFrame with columns: <code>source</code>, <code>series_id</code>, <code>name</code>, <code>description</code>, <code>frequency</code>, <code>unit</code>.</p> <p>get_available_sources <pre><code>def get_available_sources() -&gt; DataObject\n</code></pre></p> <p>Lookup Hawk IDs from tickers.</p> <pre><code>from hawk_sdk.api import System\nsystem = System(environment=\"production\")\n</code></pre> <p>get_hawk_ids <pre><code>def get_hawk_ids(tickers: List[str]) -&gt; DataObject\n</code></pre></p> Parameter Type Description <code>tickers</code> <code>List[str]</code> Ticker symbols to lookup <p>All API methods return a <code>DataObject</code> with these methods:</p> Method Description <code>to_df()</code> Convert to pandas DataFrame <code>to_csv(filename)</code> Export to CSV <code>to_xlsx(filename)</code> Export to Excel <code>show(n=5)</code> Print first n rows"},{"location":"system_usage/","title":"System","text":""},{"location":"system_usage/#lookup-hawk-ids","title":"Lookup Hawk IDs","text":"<pre><code>from hawk_sdk.api import System\n\nsystem = System()\n\nresponse = system.get_hawk_ids(\n    tickers=[\"CL00-USA\", \"JBT00-OSE\", \"SFC00-USA\", \"FGBS00-EUR\"]\n)\n\ndf = response.to_df()\nresponse.show()\nresponse.to_csv(\"hawk_ids.csv\")\n</code></pre>"},{"location":"universal_supplemental_usage/","title":"Universal Supplemental","text":"<p>Supplemental data is not tied to hawk_ids. It's identified by a combination of <code>source</code> (e.g., <code>eia_petroleum</code>, <code>fred</code>) and <code>series_id</code> (e.g., <code>WCESTUS1</code>).</p>"},{"location":"universal_supplemental_usage/#list-available-sources","title":"List Available Sources","text":"<pre><code>from hawk_sdk.api import UniversalSupplemental\n\nsupplemental = UniversalSupplemental()\n\nsources = supplemental.get_available_sources()\nsources.show()\n</code></pre>"},{"location":"universal_supplemental_usage/#list-available-series","title":"List Available Series","text":"<pre><code>from hawk_sdk.api import UniversalSupplemental\n\nsupplemental = UniversalSupplemental()\n\n# All series\nall_series = supplemental.get_all_series()\nall_series.show(n=20)\n\n# Series for a specific source\neia_series = supplemental.get_all_series(source=\"eia_petroleum\")\neia_series.show()\n</code></pre> <p>Output columns: <code>source</code>, <code>series_id</code>, <code>name</code>, <code>description</code>, <code>frequency</code>, <code>unit</code>.</p>"},{"location":"universal_supplemental_usage/#query-data-date-range","title":"Query Data (Date Range)","text":"<pre><code>from hawk_sdk.api import UniversalSupplemental\n\nsupplemental = UniversalSupplemental()\n\nresponse = supplemental.get_data(\n    sources=[\"eia_petroleum\"],\n    series_ids=[\"WCESTUS1\", \"WCRFPUS2\"],\n    start_date=\"2024-01-01\",\n    end_date=\"2024-12-31\"\n)\n\ndf = response.to_df()\nresponse.show()\n</code></pre> <p>Output columns: <code>source</code>, <code>series_id</code>, <code>series_name</code>, <code>record_timestamp</code>, <code>value</code>, <code>char_value</code>.</p>"},{"location":"universal_supplemental_usage/#query-all-data-by-source","title":"Query All Data by Source","text":"<p>Fetch all series for a source without specifying individual series_ids:</p> <pre><code>response = supplemental.get_data_by_source(\n    sources=[\"eia_petroleum\"],\n    start_date=\"2024-01-01\",\n    end_date=\"2024-12-31\"\n)\n</code></pre>"},{"location":"universal_supplemental_usage/#latest-data","title":"Latest Data","text":"<p>Get the most recent data point for each series:</p> <pre><code>response = supplemental.get_latest_data(\n    sources=[\"eia_petroleum\"],\n    series_ids=[\"WCESTUS1\", \"WCRNTUS2\", \"WCRFPUS2\"]\n)\n\nresponse.show()\n</code></pre>"},{"location":"universal_usage/","title":"Universal","text":""},{"location":"universal_usage/#query-data-date-range","title":"Query Data (Date Range)","text":"<pre><code>from hawk_sdk.api import Universal\n\nuniversal = Universal()\n\nresponse = universal.get_data(\n    hawk_ids=[1, 2, 3],\n    field_ids=[1, 4, 5],\n    start_date=\"2024-04-01\",\n    end_date=\"2024-10-01\",\n    interval=\"1d\"\n)\n\ndf = response.to_df()\nresponse.show()\n</code></pre>"},{"location":"universal_usage/#query-snapshot-point-in-time","title":"Query Snapshot (Point-in-Time)","text":"<p>Use <code>interval=\"snapshot\"</code> to get the most recent data up to <code>end_date</code>:</p> <pre><code>response = universal.get_data(\n    hawk_ids=[1, 2, 3],\n    field_ids=[17, 18, 19, 20, 21],\n    start_date=\"\",\n    end_date=\"2024-12-01 15:00:00\",\n    interval=\"snapshot\"\n)\n</code></pre>"},{"location":"universal_usage/#latest-snapshot","title":"Latest Snapshot","text":"<p>Get the most recent data available (no timestamp needed):</p> <pre><code>response = universal.get_latest_snapshot(\n    hawk_ids=[1, 2, 3],\n    field_ids=[17, 18, 19, 20, 21]\n)\n</code></pre> <p>Output columns: <code>date</code>, <code>hawk_id</code>, <code>ticker</code>, plus one column per field. Missing values are <code>NaN</code>.</p>"},{"location":"universal_usage/#list-all-fields","title":"List All Fields","text":"<pre><code>from hawk_sdk.api import Universal\n\nuniversal = Universal()\nfields = universal.get_all_fields()\nfields.show(n=50)\n</code></pre>"},{"location":"universal_usage/#lookup-field-ids-by-name","title":"Lookup Field IDs by Name","text":"<pre><code>from hawk_sdk.api import Universal\n\nuniversal = Universal()\nfield_lookup = universal.get_field_ids(\n    field_names=[\"open_1d\", \"close_1d\", \"volume_1d\"]\n)\nfield_lookup.show()\n</code></pre>"}]}